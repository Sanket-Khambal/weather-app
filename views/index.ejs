<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title>Weather</title>
</head>
<body>
    <header><h1>Weather Today</h1></header>
    <h3>Enter the city you want to check the weather for</h3>
    <form action="/" method="post">
        <input type="text" name="location" id="user-location" required >
        <button>Get current weather</button>
    </form>
    <% if(typeof(weather) && typeof(weather_2) !== 'undefined'){ %>
        <p><%= weather %></p>
        <p class="humidity"><%= weather_2 %></p>
    <% } %>

    <% if(typeof(error) !== 'undefined'){ %>
        <p><%= error %></p>
    <% } %>
    
</body>
</html>